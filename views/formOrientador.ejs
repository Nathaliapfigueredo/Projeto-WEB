<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cadastro de Orientador</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      padding-top: 70px; /* espaço pro header fixo */
      background-color: #f8f9fa;
    }

    h1 {
      color: #171440;
      text-align: center;
      margin-bottom: 30px;
    }

    form {
      background-color: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
    }

    label {
      color: #171440;
      font-weight: 600;
    }

    .btn-custom {
      background-color: #171440;
      color: white;
      font-weight: bold;
    }

    .btn-custom:hover {
      background-color: #ecac40;
    }
  </style>
</head>
<body>

  <%- include('partials/header') %>

  <div class="container">
    <h1>Cadastro de Dados do Orientador</h1>

    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <form action="/api/orientador" method="POST">
          <input type="hidden" name="id_account" value="<%= id_account %>" />

          <div class="mb-3">
            <label for="expertise" class="form-label">Expertise</label>
            <input type="text" class="form-control" id="expertise" name="expertise" required />
          </div>

          <div class="mb-3">
            <label for="bio" class="form-label">Bio</label>
            <textarea class="form-control" id="bio" name="bio" rows="4" required></textarea>
          </div>

          <div class="mb-3">
            <label for="lattes_link" class="form-label">Link do Lattes</label>
            <input type="url" class="form-control" id="lattes_link" name="lattes_link" />
          </div>

          <div class="mb-3">
            <label for="retorno_agendamento" class="form-label">Aceita Agendamentos?</label>
            <select class="form-select" id="retorno_agendamento" name="retorno_agendamento" required>
              <option value="true">Sim</option>
              <option value="false">Não</option>
            </select>
          </div>

          <button type="submit" class="btn btn-custom w-100">Finalizar Cadastro</button>
        </form>
      </div>
    </div>
  </div>

  <!-- Bootstrap Bundle JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
